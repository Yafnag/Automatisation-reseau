dhcp_dict:
    default-lease-time : 600
    max-lease-time  : 7200
    option domain-name-servers  : "8.8.8.8, 8.8.4.4"
    

vlan_dict:
    auto : "vlan"
    iface : "vlan"
    inet : "static"
    vlan-raw-device : "ens33"
    
iface_file_name : "interfaces"
dhcpd_file_name : "dhcpd.conf"

#Chemin vers le fichier de configuration interfaces du systeme d'exploitation
path_iface: "./"

#Chemin vers le fichier de configuration "dhcpd.conf"
path_dhcpd : "./"